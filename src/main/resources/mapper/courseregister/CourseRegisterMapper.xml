<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.goott5.lms.courseregister.mapper.CourseRegisterMapper">

  <insert id="insertCourseSchedule">

    insert into course_schedule(subject_id, course_id, class_date, period, period_start_time, period_end_time)
    values
    <foreach collection="list" item="item" separator=",">

      (#{item.subjectId}, #{item.courseId}, #{item.classDate}, #{item.period}, #{item.periodStartTime}, #{item.periodEndTime})

    </foreach>


  </insert>

</mapper>